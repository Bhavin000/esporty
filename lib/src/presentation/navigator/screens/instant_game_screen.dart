import 'package:flutter/material.dart';

class InstantGameScreen extends StatelessWidget {
  const InstantGameScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
